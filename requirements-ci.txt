# Pinned CI tooling. SINGLE SOURCE OF TRUTH for .github/workflows/ci.yml.
#
# Why pinned at all: on 2026-07-23 ruff 0.16.0 moved rules into its stable set
# and failed the sibling repo's scheduled deploy two nights running, on code
# nobody had touched. An unpinned tool turns an upstream release into a red
# build with no local cause, and pytest is exactly the same shape.
#
# Bumping: change the pin here ONLY, in a commit that also clears whatever the
# new version flags. Never bump it to make a red build go green.
#
# ruff added 2026-07-25, once the 4 pre-existing E741 findings were cleared. The
# rename that cleared them is proven behavior-preserving: the built zip still
# reproduces the zip_sha256 recorded in skin_build.lock. Rule set is declared in
# ruff.toml so a ruff release cannot change what "clean" means here.
pytest==9.1.1
ruff==0.15.22
